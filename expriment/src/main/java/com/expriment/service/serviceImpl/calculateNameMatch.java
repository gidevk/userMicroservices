package com.expriment.service.serviceImpl;

public class calculateNameMatch {
    // Online Java Compiler
// Use this editor to write, compile and run your Java code online

        // public static void main(String[] args) {
        //     System.out.println("Hello, World!");
        // }
//        public static void main(String[] args) {
//            String name1 = "indradev kumar";
//            String name2 = "indradev kumar choudhary";
//            double matchPercentage = calculateNameMatchPercentage(name1, name2);
//            System.out.println("Match Percentage: " + matchPercentage + "%");
//        }

      /*  public static double calculateNameMatchPercentage(String name1, String name2) {
            JaroWinklerDistance jaroWinklerDistance = new JaroWinklerDistance();
            double distance = jaroWinklerDistance.apply(name1, name2);
            double matchPercentage = (1 - distance) * 100.0;
            return matchPercentage;
        }*/
        // public static double calculateNameMatchPercentage(String name1, String name2) {
        //     int maxLength = Math.max(name1.length(), name2.length());
        //     int matchingChars = 0;

        //     for (int i = 0; i < maxLength; i++) {
        //         if (i < name1.length() && i < name2.length() && name1.charAt(i) == name2.charAt(i)) {
        //             matchingChars++;
        //         }
        //     }

        //     return (double) matchingChars / maxLength * 100.0;
        // }





}
